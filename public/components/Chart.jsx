var React = require('react');
var PropTypes = React.PropTypes;

var Chart = React.createClass({
  
  componentDidMount: function () {
    //use component cycle to hook into d3

  },

  render: function () {
    return (
      <div>
        Hello world
      </div>
    );
  }
});

module.exports = Chart;
